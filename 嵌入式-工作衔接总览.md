# 嵌入式-工作衔接总览

## 原始问题

怎样把当前这套笔记目录整理成一张真正服务嵌入式工作的知识地图，让我后续学习、项目开发、代码阅读、排障和面试都能顺着同一套结构推进，而不是每次都从零找资料、临时想目录？

## 先给结论

要实现“嵌入式工作的无压力衔接”，关键不是把所有资料堆在一起，而是建立一套稳定的主题地图，让每类问题都有固定落点、固定排查主线和固定扩展路径。

当前目录体系按下面这条主线组织：

1. `C_C++`：语言与内存基础，解决“写得出来、看得懂、改得动”。
2. `硬件`：电平、时钟、复位、寄存器、引脚、总线这些底层本质，解决“知道真实硬件发生了什么”。
3. `外设`：GPIO、UART、I2C、SPI、ADC、PWM 这类外设主题本身，解决“会用外设并能讲清主线”。
4. `通信协议`：串口协议、Modbus、CAN、以太网等通信层问题，解决“设备之间怎么稳定交换数据”。
5. `RTOS`：任务、队列、同步、定时器、中断配合，解决“单片机和实时系统怎么把功能组织起来”。
6. `Linux`：设备树、用户态、内核态、系统基础，解决“嵌入式 Linux 平台怎么工作”。
7. `驱动开发`：platform、I2C、SPI、字符设备、内核接口、资源管理，解决“怎么把硬件接进系统”。
8. `板级与启动`：Bootloader、启动流程、板级 bring-up、时钟树、电源时序、烧录升级，解决“板子怎么真正跑起来”。
9. `工具链与调试`：编译、烧录、调试器、示波器、逻辑分析仪、日志、core dump、性能分析，解决“问题怎么定位和验证”。
10. `项目与架构`：模块边界、接口设计、状态机、错误处理、版本演进、量产和维护，解决“怎么把代码做成可交付系统”。

这套目录不是为了“看起来完整”，而是为了让你以后面对一个真实问题时，能迅速知道它归哪类、先查什么、下一个相邻主题是什么。

## 这个总览解决什么问题

这张总览主要解决下面几类现实问题：

1. 新增笔记时，不知道应该放到哪个目录。
2. 工作里遇到问题时，只知道现象，不知道该先从硬件、驱动、RTOS 还是协议层切入。
3. 学习嵌入式时容易东一块西一块，缺少从基础到项目落地的连续主线。
4. 面试、工作交接和独立承担任务时，知识点很多，但缺少能快速调用的目录导航。

## 工作场景到目录的映射

可以先按下面这个思路快速定位：

1. 如果问题在语法、内存、对象生命周期、资源管理，优先看 `C_C++`。
2. 如果问题在电平、复位、时钟、pinmux、寄存器、原理图，优先看 `硬件`。
3. 如果问题在 GPIO、UART、I2C、SPI、ADC、PWM 这些外设本身，优先看 `外设`。
4. 如果问题在设备之间怎么通信、帧格式、校验、超时、重传，优先看 `通信协议`。
5. 如果问题在任务调度、同步、队列、优先级反转、中断与任务配合，优先看 `RTOS`。
6. 如果问题在设备树、内核日志、用户态命令、系统行为，优先看 `Linux`。
7. 如果问题在 probe、资源申请、IRQ、总线匹配、`devm_*`、驱动模型，优先看 `驱动开发`。
8. 如果问题在启动失败、Bootloader、烧录、板卡点不亮、DDR 或存储初始化，优先看 `板级与启动`。
9. 如果问题在如何抓日志、打断点、抓波形、看寄存器、分析崩溃，优先看 `工具链与调试`。
10. 如果问题在模块拆分、接口稳定性、可维护性、版本演进、量产交付，优先看 `项目与架构`。

## 推荐学习与工作衔接顺序

下面这条顺序不是唯一正确答案，但很适合作为稳定主线：

1. `C_C++` 先打牢，保证能独立写和改代码。
2. `硬件` 和 `外设` 并行推进，建立“代码最终控制了什么”的直觉。
3. `通信协议` 跟外设一起学，把“单点控制”扩展到“设备通信”。
4. 做 MCU 方向时尽快补 `RTOS`，把功能组织成系统。
5. 做 Linux 方向时补 `Linux` 和 `驱动开发`，把板级描述、总线、驱动模型串起来。
6. 一旦开始碰真实板卡，就同步补 `板级与启动` 和 `工具链与调试`。
7. 当能独立完成一个功能闭环后，开始系统补 `项目与架构`，把代码从“能跑”升级到“能交付、能维护、能扩展”。

## 当前稳定目录

1. `C_C++`
2. `硬件`
3. `外设`
4. `通信协议`
5. `RTOS`
6. `Linux`
7. `驱动开发`
8. `板级与启动`
9. `工具链与调试`
10. `项目与架构`

## 后续建议优先补齐的主题

如果目标是尽快服务嵌入式工作，建议优先继续补这些主题：

1. `外设-I2C通信基础`
2. `外设-SPI通信基础`
3. `RTOS-任务间通信`
4. `RTOS-中断与任务协作`
5. `通信协议-CRC与校验`
6. `驱动开发-I2C驱动基础`
7. `板级与启动-板级BringUp主线`
8. `工具链与调试-GDB调试基础`
9. `项目与架构-模块边界划分`
10. `项目与架构-错误处理与日志策略`

## 关键要点

1. 目录整理的目标不是形式整齐，而是让工作中的问题有固定落点。
2. 每个目录都要服务真实场景，而不是只做主题标签。
3. 以后新增笔记时，优先看“问题主线是什么”，再决定放在哪个目录。
4. 这张总览本质上是一张嵌入式工作知识地图，不是一份一次性学习清单。

## 关联笔记

1. `README`
2. `硬件-基础知识`
3. `外设-总览`
4. `Linux-总览`
5. `RTOS-总览`
6. `驱动开发-总览`

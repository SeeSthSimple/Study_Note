# 硬件基础入门 01：从电源开始

## 原始问题

先从硬件电源开始吧，我所能接触到的只有芯片对电压有要求，不能过高/低，电路知识甚少，帮我保姆化扩充硬件的基础知识。

## 一句话先建立感觉

对嵌入式系统来说，电源就像“供水系统”。

- 电压像水压
- 电流像水流量
- 功率像总耗水能力
- 负载像用水设备

芯片能不能正常工作，最先看的往往不是程序，而是“电源有没有供对、供稳、供够”。

## 一、电源里最重要的三个量

### 1. 电压（V）

电压可以理解成“推动电流流动的能力”。

常见电压有：

- `5V`
- `3.3V`
- `1.8V`
- `12V`

对芯片来说，电压必须在允许范围内。

例如：

- 一个 `3.3V` 的 MCU，正常工作电压可能是 `3.0V ~ 3.6V`
- 如果只给到 `2.5V`，它可能不启动、跑飞、串口乱码
- 如果给到 `5V`，轻则发热，重则直接损坏

先记住一句最重要的话：

**芯片不是只看“标称电压”，而是看数据手册里的“工作电压范围”和“绝对最大额定值”。**

### 2. 电流（A / mA）

电流表示“实际流过的电量大小”。

很多初学者容易误解：

- 电压是电源“给多少”
- 电流不是电源硬塞给芯片多少
- 而是芯片“按需取多少”

例如：

- 一个 `3.3V` 传感器工作时需要 `10mA`
- 你用一个最大可输出 `1A` 的 `3.3V` 电源给它供电，是可以的
- 因为它只会取自己需要的 `10mA`

但如果电源最多只能提供 `5mA`：

- 芯片想吃 `10mA`
- 电源却供不上
- 电压就可能掉下来
- 系统就会异常

所以我们常说：

**电压要匹配，电流能力要留余量。**

### 3. 功率（W）

功率表示消耗能量的快慢。

公式很重要：

`P = U × I`

例如：

- `5V × 2A = 10W`
- `3.3V × 100mA = 0.33W`

功率直接关系到：

- 电源芯片是否会过热
- 板子铜皮是否够宽
- LDO 是否能扛住压差发热
- 供电模块是否选型过小

## 二、你要先分清“谁给谁供电”

一个嵌入式板子里，常见供电路径大概是这样：

`输入电源 -> 电源转换电路 -> 各路芯片`

例如：

`12V 适配器 -> DC-DC 降到 5V -> LDO 降到 3.3V -> MCU/传感器`

这里有三个概念：

### 1. 输入电源

外部进来的电，可能是：

- `USB 5V`
- 锂电池 `3.7V`
- 适配器 `12V`
- 工业电源 `24V`

### 2. 电源转换

外部电压通常不能直接给芯片用，所以需要转换。

常见有：

- `LDO`：线性稳压
- `DC-DC`：开关电源

### 3. 负载

最终用电的器件，比如：

- MCU
- 传感器
- 屏幕
- Wi-Fi 模块
- 继电器
- 电机驱动

## 三、LDO 和 DC-DC，先建立最基础印象

### 1. LDO 是什么

LDO 可以理解成“简单但会发热的降压器”。

特点：

- 电路简单
- 输出比较干净
- 噪声相对小
- 适合小电流场景
- 输入输出压差越大，发热越明显

比如：

- 输入 `12V`
- 输出 `3.3V`
- 电流 `100mA`

功耗大约是：

`(12 - 3.3) × 0.1 = 0.87W`

对一个小封装芯片来说，`0.87W` 已经很容易烫了。

所以：

**LDO 不是不能用，而是不适合“大压差 + 大电流”。**

### 2. DC-DC 是什么

DC-DC 可以理解成“更高效的电压变换器”。

特点：

- 效率高
- 发热小
- 适合电流较大场景
- 电路比 LDO 复杂
- 会带来开关噪声

典型场景：

- `12V -> 5V`
- `24V -> 5V`
- 电池供电产品里为了省电

初学阶段先这样记：

- 小电流、图省事、追求干净：优先想 `LDO`
- 大压差、大电流、讲效率：优先想 `DC-DC`

## 四、为什么“电压对了”系统还是会出问题

这部分最关键，因为很多软件同学会卡在这里。

### 1. 电流不够

表面看是 `3.3V`，但一上电就掉到 `2.8V`，原因常常是供电能力不够。

常见现象：

- MCU 反复重启
- Wi-Fi 模块一发射就死机
- 屏幕一亮系统就复位
- 电机一转，主控重启

### 2. 电压不稳


不是长期高低不对，而是瞬间抖动、跌落、尖峰。

常见来源：

- 负载瞬时电流太大
- 电源线过长
- 去耦电容不够
- 布线太细
- 地线处理不好

### 3. 噪声干扰

有些芯片不仅要“有电”，还要“干净的电”。

比如：

- ADC 采样
- 射频模块
- PLL
- 高精度传感器

如果电源噪声大，可能会出现：

- ADC 结果乱跳
- 时钟不稳
- 无线性能变差

### 4. 上电时序不对

有些芯片不是电到了就行，还要求“谁先上电，谁后上电”。

例如：

- 核心电压先到
- IO 电压后到
- 复位信号延后释放

时序不对可能导致：

- 芯片不起机
- 外设识别失败
- 偶发性异常

## 五、你必须认识的几个硬件基础名词

### 1. GND

`GND` 是地，是整个电路共同参考点。

它不是“可有可无的负极线”，而是：

- 电流回路的一部分
- 电压测量的参考基准
- 信号能否正确识别的重要前提

很多问题本质上不是“没电”，而是“地没接好”。

### 2. VCC / VDD / 3V3 / 5V

这些通常表示电源网络名称。

你可以先简单理解为：

- `5V`：5伏电源
- `3V3`：3.3伏电源
- `VCC/VDD`：芯片供电脚

不同芯片命名不同，不必一开始就纠结历史来源。

### 3. 去耦电容 / 旁路电容

这是每个初学者必须知道的东西。

它们通常放在芯片电源脚旁边，作用可以先粗略理解为：

- 局部小电池
- 吸收高频噪声
- 在芯片瞬间要电流时先顶一下

常见搭配：

- 一个 `0.1uF`
- 再加一个 `1uF` 或 `4.7uF`

放置原则：

- **尽量靠近芯片电源脚**

不是“板子上有电容就行”，距离远了效果会明显变差。

### 4. 滤波

滤波就是让电更平稳、把噪声压下去。

常见器件：

- 电容
- 电感
- 磁珠

初学时先记住：

- 电容擅长旁路高频噪声和稳定局部电压
- 磁珠常用来隔离高频干扰
- 电感常用于开关电源储能和滤波

### 5. 复位

复位可以理解为“让芯片重新回到起跑线”。

低电压、上电不稳、时钟异常，都可能导致复位。

你以后经常会碰到：

- 上电复位
- 看门狗复位
- 欠压复位
- 外部按键复位

## 六、实际设计里最常见的供电链路长什么样

### 场景 1：USB 供电的小板子

`USB 5V -> LDO 3.3V -> MCU + 传感器`

适合：

- 小电流
- 开发板
- 简单传感器节点

### 场景 2：12V 适配器输入

`12V -> DC-DC 5V -> LDO 3.3V`

思路是：

- 先用 `DC-DC` 高效地把高电压降下来
- 再用 `LDO` 给敏感芯片一个更干净的电源

### 场景 3：电池供电

`锂电池 -> 保护电路 -> DC-DC/LDO -> 系统负载`

这里还会涉及：

- 充电管理
- 电池保护
- 低功耗设计
- 欠压检测

## 七、作为软件/嵌入式初学者，你最该先建立的判断习惯

### 1. 先看芯片手册的供电范围

至少看三个信息：

- 推荐工作电压范围
- 绝对最大额定值
- 典型工作电流

### 2. 不要只看“有没有电”，要看“稳不稳”

万用表能看平均值，但很多异常是瞬态问题。

所以以后你会逐步接触：

- 万用表：看静态电压
- 示波器：看波形、纹波、跌落、尖峰

### 3. 负载变化大的系统要特别小心

例如：

- 无线发射
- 电机启动
- 背光打开
- 继电器吸合

这些都会带来瞬时大电流。

### 4. 电源问题经常伪装成软件问题

比如：

- 程序偶发跑飞
- 串口偶发乱码
- I2C 偶发超时
- Flash 写入失败
- 传感器数据偶发异常

这些不一定是代码错了，可能是供电在某个瞬间出了问题。

## 八、你现在最需要会的基础公式和判断

### 1. 欧姆定律

`U = I × R`

用于理解：

- 电阻分压
- 上拉下拉
- 电流大小

### 2. 功率公式

`P = U × I`

用于估算：

- 电源容量够不够
- 稳压器会不会过热

### 3. LDO 发热粗算

`P_loss = (Vin - Vout) × Iout`

这个你以后会经常用。

如果结果比较大，就要警惕：

- 芯片发热
- 效率低
- 可能需要换成 `DC-DC`

## 九、你可以先把硬件世界粗分成这几层

为了不一下子学乱，建议你先按层理解：

### 第 1 层：供电

- 电压
- 电流
- 功率
- 稳压
- 去耦

### 第 2 层：基础器件

- 电阻
- 电容
- 电感
- 二极管
- 三极管
- MOS 管

### 第 3 层：连接和信号

- 高低电平
- 上拉下拉
- 输入输出
- 开漏推挽

### 第 4 层：接口总线

- UART
- SPI
- I2C
- CAN

### 第 5 层：系统问题

- EMI
- 地线
- 时序
- 复位
- 可靠性

你现在从第 1 层开始是非常对的。

## 十、给你一个最实用的“上板先看什么”清单

以后拿到一块板子，先看这些：

1. 输入电压是多少
2. 板上有哪些电源轨，比如 `12V`、`5V`、`3.3V`、`1.8V`
3. 每一路电压是否正常
4. 上电时有没有明显掉压
5. 哪个芯片最热
6. MCU 的供电脚附近有没有去耦电容
7. 大电流器件和主控是不是共用了一条很弱的供电线
8. 地是否共地

## 十一、初学者最容易踩的坑

- 把 `5V` 直接接到 `3.3V IO`
- 以为“电源能输出 2A”就会把芯片烧掉
- 只测静态电压，不看动态跌落
- 忽略 `GND`
- 去耦电容离芯片太远
- 用 `LDO` 硬降大压差大电流，结果发热严重
- 电机、继电器、无线模块和主控共电源却没做好隔离

## 十二、这一阶段你先记住的核心结论

1. 对芯片来说，**电压必须对**
2. 对系统来说，**电流必须够**
3. 对稳定性来说，**电源必须稳**
4. 对可靠性来说，**去耦、滤波、地线、时序都重要**
5. 很多“玄学 bug”本质是电源问题

## 建议你下一步接着学什么

按这个顺序最顺：

1. 电阻、电容、电感分别是干什么的
2. 上拉、下拉、高低电平
3. 三极管和 MOS 管的基本作用
4. LDO、DC-DC、电源纹波、去耦
5. 再进入 `UART/SPI/I2C`

## 给你的一个通俗收尾

如果把嵌入式系统比作一个人：

- 电源是血液系统
- 时钟是心跳节奏
- 复位是重新清醒
- IO 是手脚
- 通信总线是嘴巴和耳朵
- 软件是大脑里的逻辑

所以硬件入门时，先学电源是很好的起点。因为只要“血液系统”不稳，后面所有现象都可能是假的。


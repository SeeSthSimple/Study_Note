# C/C++ 嵌入式学习档案

## 作用
用于快速恢复当前学习状态，支持在新会话中无缝续训。
要求：
- 只保留高价值信息
- 不记录无意义寒暄
- 新会话读取本文件后，应能直接接着训练

## 学习者画像
- 基础：知道部分 C 语法，能看懂基础代码
- 当前主要卡点：写代码时容易卡在“如何下手”，指针、结构体、边界处理、接口设计不够稳
- 目标：3 个月内能独立写中小型嵌入式模块，逐步达到中高级嵌入式工程师所需的代码组织与调试能力

## 教学约定
- 先讲思路，不直接给完整答案
- 优先追问“应该怎么想”
- 学不会时按层级逐步提示
- 最后再给标准答案和总结
- 训练强度随能力提升动态升级
- 每次训练优先培养：拆题能力、边界意识、接口设计能力、调试意识、模块化思维

## 当前训练等级
- 等级：L2 初段
- 相对位置：已脱离纯语法入门，正在从“会一点 C”过渡到“能分析并设计基础模块”
- 进度估计：
  - 整体 3 个月目标进度约 20%~25%
  - 第一阶段基础补稳进度约 40%~50%

## 已掌握内容
### 1. 指针与内存基础
- 知道普通变量存值，指针变量存地址
- 知道 `p`、`*p`、`&p` 的基本含义
- 能理解“指针 = 地址 + 类型信息”
- 知道指针类型决定：
  - 如何解释内存
  - 一次访问多少字节
  - 指针运算移动多远
- 知道传值和传地址的本质区别
- 知道为什么 `swap`、`set_value` 这类函数需要指针才能修改外部变量

### 2. 数组与指针关系
- 知道数组是一段连续内存
- 知道数组名在多数表达式中会退化为首元素地址
- 能理解：
  - `arr[i] == *(arr + i)`
  - `p[i] == *(p + i)`
- 知道 `arr + 1` 是地址移动，不是值加 1
- 能区分：
  - `arr`
  - `&arr[0]`
  - `&arr`
- 初步理解：`arr` 和 `&arr[0]` 常常可用于同一访问场景，但 `&arr` 是“整个数组的地址”，类型不同
- 知道 `sizeof(arr)` 和 `sizeof(&arr)` 不同的本质原因
- 知道数组作为函数参数时会退化为指针，因此函数内不能靠 `sizeof(参数)` 求原数组长度

### 3. 字符数组、字符串、缓冲区
- 知道字符数组不一定是字符串
- 知道字符串依赖 `\0` 结束
- 知道：
  - `sizeof(buf)` 看容量/对象大小
  - `strlen(buf)` 看有效字符串长度，不包含 `\0`
- 知道“让字符串变成空串”和“清空整个缓冲区”不是同一件事
- 知道字符串传参只传首地址，不带长度信息，因此函数需要额外传长度以防越界
- 已具备基础边界意识，能意识到 `<= len` 这类循环条件可能导致越界写入

### 4. 结构体与结构体指针
- 知道结构体用于组织多个相关状态数据
- 知道结构体变量传值时会产生副本，传指针时可以修改原对象
- 知道：
  - `p->member` 等价于 `(*p).member`
  - `.` 用于结构体对象
  - `->` 用于结构体指针
- 知道结构体整体是一块连续内存区域，但成员间可能因对齐存在填充字节
- 已开始理解“结构体 + 函数 = 小模块”的思维

### 5. 缓冲区与队列的模块意识
- 能理解简单缓冲区结构体中：
  - `data[]` 代表存储区
  - `len` 代表当前有效数据长度
- 知道容量和当前有效长度不是同一概念
- 理解 `push` 时不改变容量，而是写入 `data[len]` 并更新 `len`
- 初步理解队列应通过状态索引来管理数据流动，而不是频繁搬移数据
- 已接触环形队列核心成员：
  - `head`
  - `tail`
  - `count`
- 知道入队/出队本质：
  - 入队动 `tail`
  - 出队动 `head`
  - `count` 反映当前元素个数
- 能理解为什么 `queue_pop` 常设计为“返回状态码 + 输出参数”

## 当前易错点
- 仍会偶尔把“地址”和“值”混着说
- 容易把“元素值”和“元素地址”混淆，例如把 `arr + 1` 说成元素值本身
- 下标偶尔会数错一位，例如 `arr[2]` 与第二个元素混淆
- 对数组退化规则已有理解，但表述还不够严谨
- 设计队列操作时，偶尔会把“按 head/tail 直接定位”误想成“遍历查找”
- 对 `const`、函数指针、回调函数、状态机、位运算还未正式展开系统训练

## 当前能力评价
- 优点：
  - 愿意从“内存本质”角度理解问题
  - 已开始具备边界意识和接口意识
  - 能从结构体过渡到模块设计，而不只停留在语法层
- 目前短板：
  - 精准表达和严格推演还不稳定
  - 容易在细节处错一位或概念混用
  - 尚未进入完整模块代码实现与调试阶段

## 下一阶段训练顺序
### 近期优先
1. 巩固指针、数组、结构体之间的关系，继续纠正“地址/值/类型”混淆
2. 继续补强字符串、缓冲区、边界检查思维
3. 讲透函数指针与回调函数
4. 讲透 `const`、`static`、`volatile`
5. 讲透枚举、状态机、位运算

### 之后进入
1. 环形队列完整模块设计
2. 串口收发缓冲区
3. 按键消抖模块
4. 简化版驱动接口设计
5. 多文件工程组织

## 建议的后续训练入口
下次新会话可直接从以下主题继续：
- 函数指针是什么，和普通指针的相同点与不同点
- 回调函数为什么常用于嵌入式事件处理
- `const / static / volatile` 在嵌入式中的实际意义
- 环形队列中 `head/tail/count` 的完整推演

## 最近一次结论
- 学习者已基本建立“值、地址、类型、解引用”的核心链条
- 已能理解数组退化、字符串与缓冲区区别、结构体指针、基础模块接口设计
- 当前最适合继续从“函数指针/回调函数”切入，开始从基础语法过渡到更贴近嵌入式工程的知识点

## 给下一位教练的说明
请继续保持以下教学风格：
- 先讲思路，不要一上来给完整答案
- 先追问学习者“应该怎么想”
- 如果不会，再逐步提示
- 最后再给标准答案和总结
- 避免重复基础定义，优先围绕学习者已经学过的内容继续拔高
- 发现学习者能力提升时，适度提高题目难度，但仍要控制节奏

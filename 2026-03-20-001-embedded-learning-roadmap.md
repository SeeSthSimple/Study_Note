# 原始问题

在 `D:\CodeRep\embedded-qa-records` 文件夹下生成我要学习的嵌入式知识文件。

---

# 嵌入式学习知识地图

> 目标不是“知道名词”，而是尽快达到“能独立看懂代码、能写驱动、能定位问题、能做小项目”的程度。
> 学习顺序要遵守一个原则：先打牢底层，再上 RTOS 和项目架构，否则容易只会调库，不会分析问题。

---

## 0. 你要学的不是一堆零散知识，而是 8 个层次

1. **C 语言与底层编程基础**
2. **MCU 架构与启动流程**
3. **常用外设与寄存器操作**
4. **中断、定时、并发与实时性**
5. **通信协议与驱动设计**
6. **内存、编译、链接、启动文件**
7. **RTOS / FreeRTOS**
8. **调试、稳定性、功耗、Bootloader、项目化能力**

你后面做的大多数项目，本质上都在回答这几个问题：

- 外设怎么初始化
- 数据怎么进来、怎么出去
- 任务和中断怎么协作
- 内存和时序是否安全
- 出问题后如何定位

---

## 1. 必学知识清单

### 1.1 C 语言与嵌入式 C

这一层不要求你刷算法，但要把“项目里真会用到”的东西吃透：

- 指针、二级指针、函数指针
- `const`、`volatile`、`static`、`extern`
- 结构体、联合体、位域
- 数组与指针的区别
- 宏、条件编译、头文件组织
- 内存布局：栈、堆、全局区、静态区、只读区
- 字节序、对齐、sizeof、offsetof
- 回调、状态机、环形缓冲区

学到什么程度算合格：

- 能自己写一个环形缓冲区
- 能读懂驱动里的寄存器宏和结构体
- 能解释 `volatile` 为什么不能代替互斥
- 能判断一个变量为什么要放全局、静态或局部

### 1.2 MCU 基础架构

要掌握 MCU 不是“会点灯”，而是知道芯片上电后到底发生了什么：

- Cortex-M 基本结构：内核、寄存器组、异常模型
- 存储器映射：Flash、SRAM、外设寄存器
- 启动流程：复位、向量表、启动文件、`main`
- 时钟系统：主时钟、分频、外设时钟
- GPIO 工作模式
- 看手册的方法：参考手册、数据手册、芯片勘误

学到什么程度算合格：

- 能说清楚“程序上电后是怎么跑到 `main` 的”
- 能理解外设寄存器地址为什么能像变量一样读写
- 能独立配置时钟和 GPIO

### 1.3 常用外设

这是做项目最常见的一层：

- GPIO
- UART / USART
- 定时器 TIM
- 外部中断 EXTI
- ADC
- PWM
- DMA
- Watchdog
- RTC

学习目标：

- 不只是会调 HAL 函数，要知道关键寄存器含义
- 知道外设的数据流和中断触发条件
- 能把“初始化”“收发”“错误处理”拆开设计

### 1.4 中断、实时性、并发

这是嵌入式和普通应用开发差别最大的地方之一：

- 中断触发流程
- NVIC 优先级
- ISR 设计原则
- 临界区
- 原子性
- 共享资源保护
- 主循环与 ISR 协作
- 任务与 ISR 协作
- 抖动、延迟、最坏响应时间

学到什么程度算合格：

- 你能解释“为什么 ISR 里不能乱 `printf`”
- 你能写出 ISR + 环形缓冲区 + 主循环解析的典型模型
- 你能分析优先级配置错误导致的时序问题

### 1.5 通信协议与驱动设计

协议不是死记时序图，而是要学会抽象数据流和接口边界：

- UART
- I2C
- SPI
- CAN
- RS-485
- 如果项目需要，再补 USB / Ethernet / Modbus / MQTT

学习重点：

- 每种协议的主从关系、同步方式、错误场景
- 驱动层和业务层分离
- 接收缓冲、帧解析、超时、重发、校验
- 面向接口设计，而不是把协议细节写死在业务代码里

### 1.6 编译、链接、启动与内存

这部分经常被忽略，但一到项目问题就绕不过去：

- 预处理、编译、汇编、链接流程
- `.text`、`.data`、`.bss`、`.rodata`
- 链接脚本 / scatter file 基础
- 启动文件里数据搬运和零初始化
- Map 文件怎么看
- 栈溢出、堆碎片、内存越界

学到什么程度算合格：

- 能解释全局变量为什么有的进 Flash、有的进 RAM
- 能通过 map 文件看大头内存占用
- 知道嵌入式项目为什么尽量少依赖动态内存

### 1.7 RTOS / FreeRTOS

这是“会写单片机”和“能做中大型项目”的分水岭：

- 任务、调度、优先级
- Tick、中断与调度关系
- Queue、Semaphore、Mutex、Event Group、Timer
- 任务栈
- 优先级反转
- 死锁
- ISR 和任务 API 的区别

学习目标：

- 知道什么时候该用任务，什么时候该留在中断或主循环
- 知道各种 IPC 结构的典型使用场景
- 会看任务栈、水位、CPU 占用和阻塞状态

### 1.8 调试、可靠性、功耗、Bootloader

这层决定你能不能把代码真正做成产品：

- `assert`、日志、错误码
- JTAG / SWD 调试
- 逻辑分析仪、示波器基础
- HardFault 排查
- 看门狗策略
- 软件分层与模块化
- 低功耗模式
- Bootloader 与 OTA 基础
- 版本管理、配置管理、产测思维

---

## 2. 推荐学习顺序

不要东一榔头西一棒子，推荐按下面顺序推进。

### 阶段 1：先把 C 和底层表达能力打稳

重点：

- 指针、结构体、内存布局
- 位运算、宏、状态机
- 环形缓冲区、队列、回调

必须自己动手写：

- 一个按键消抖模块
- 一个状态机
- 一个固定长度环形缓冲区

### 阶段 2：裸机最小系统能力

重点：

- 启动流程
- 时钟
- GPIO
- UART
- Timer

必须自己动手做：

- LED 闪烁
- 按键输入
- UART 收发
- 定时器周期中断

目标：

- 用最少库函数把最小系统跑起来

### 阶段 3：中断与数据通路

重点：

- NVIC
- ISR 规则
- 临界区
- 并发数据访问
- DMA 基础

必须自己动手做：

- UART 中断接收 + 环形缓冲区
- 定时采样 + 主循环处理
- 一个“中断只搬运，主循环做解析”的标准模型

### 阶段 4：协议与驱动

重点：

- I2C / SPI / CAN 至少掌握两种
- 驱动初始化、收发接口、错误处理、超时控制

必须自己动手做：

- 读一个传感器
- 写一个简单的串口协议解析器
- 把驱动层和应用层拆开

### 阶段 5：FreeRTOS

重点：

- 任务切换
- Queue / Semaphore / Mutex
- ISR 和任务协作
- 栈与优先级设计

必须自己动手做：

- 传感器采集任务
- 通信任务
- 日志任务
- 队列传递消息

### 阶段 6：项目化与产品思维

重点：

- 模块划分
- 故障定位
- 低功耗
- Bootloader / OTA
- 稳定性设计

必须自己动手做：

- 一个完整小项目
- 写清楚模块接口、状态机、错误处理和测试方法

---

## 3. 你现在最值得优先学习的主题

结合这个目录里已经有的笔记，建议你优先按下面顺序继续深化：

1. `power`
2. `gpio`
3. `interrupt`
4. `freertos`
5. `uart`
6. `timer`
7. `dma`
8. `i2c`
9. `spi`
10. `watchdog`
11. `bootloader`
12. `ota`

也就是说，你接下来最该补齐的是：

- `uart` 全链路
- `timer` 和软件定时架构
- `dma` 的使用边界
- `i2c/spi` 驱动设计
- `bootloader/ota` 的工程化认知

---

## 4. 一份可执行的 8 周学习路线

### 第 1 周

- 复习 C 语言中和嵌入式强相关的部分
- 自己写环形缓冲区、状态机、位操作工具
- 弄清 `volatile`、`static`、`extern`

### 第 2 周

- 学 MCU 启动流程、时钟、GPIO、UART
- 完成 LED、按键、串口打印

### 第 3 周

- 学中断、NVIC、定时器
- 完成 UART 中断接收、定时器周期任务

### 第 4 周

- 学 DMA、ADC、PWM
- 完成一个“采样 + 缓冲 + 处理”小实验

### 第 5 周

- 学 I2C / SPI
- 完成一个传感器驱动

### 第 6 周

- 学 FreeRTOS 基础对象
- 完成多任务消息通信实验

### 第 7 周

- 学调试、HardFault、Watchdog、内存分析
- 学会看 map 文件和任务栈

### 第 8 周

- 做一个小项目并整理文档
- 推荐项目：温湿度采集 + OLED 显示 + 串口协议 + FreeRTOS + 看门狗

---

## 5. 每个知识点都要达到“项目可用”标准

不是“看过”就算学会，至少要过这 4 关：

1. **能讲清原理**
2. **能自己写最小代码**
3. **能说出常见坑**
4. **能放进一个小项目里**

例如学 UART，不是只会 `HAL_UART_Transmit()`，而是要能回答：

- 波特率怎么来的
- RXNE / TXE / TC 分别表示什么
- 串口中断接收为什么适合配环形缓冲区
- 什么场景下该用 DMA
- 粘包、丢包、溢出怎么处理

---

## 6. 建议你重点培养的工程能力

很多人卡住，不是卡在知识点，而是卡在不会把知识落地。你要刻意训练这几项：

- 看芯片手册和原理图的能力
- 读已有工程代码的能力
- 分层写驱动的能力
- 用日志、示波器、逻辑分析仪定位问题的能力
- 把功能拆成状态机和数据流的能力
- 写“可维护代码”而不是“能跑代码”的能力

---

## 7. 学习时最常见的误区

### 误区 1：只会调库，不懂底层

后果：

- 一旦外设异常、时序不稳、数据丢失，就完全不知道怎么查

### 误区 2：急着学 RTOS，却没有中断和裸机基础

后果：

- 会写任务，不会分析优先级、临界区和 ISR 协作问题

### 误区 3：只看概念，不做实验

后果：

- 知识点停留在“眼熟”，一写代码就断层

### 误区 4：不做总结，不沉淀模板

后果：

- 下次做 UART、I2C、任务通信，还像第一次做一样慢

---

## 8. 你现在可以直接开始做的 5 个小练习

1. 写一个不依赖库的环形缓冲区
2. 写一个按键消抖 + 长按识别模块
3. 写一个 UART 中断接收 + 帧解析模块
4. 写一个定时器驱动的周期调度模块
5. 写一个 FreeRTOS 的生产者消费者实验

如果这 5 个练习你都能独立完成，基础就已经比较扎实了。

---

## 9. 后续建议记笔记的主题

后面你每学完一个模块，都建议继续在这个目录里单独沉淀一篇，例如：

- `uart-driver-design`
- `timer-and-soft-timer`
- `dma-hands-on`
- `i2c-driver-hands-on`
- `spi-driver-hands-on`
- `adc-sampling-and-filter`
- `watchdog-reset-strategy`
- `bootloader-basics`
- `hardfault-debug-guide`

---

## 10. 关联已有笔记

当前目录里已经有这些主题，可作为配套学习材料继续往下串：

- `2026-03-18-001-power-basics.md`
- `2026-03-18-002-gpio-basics.md`
- `2026-03-19-001-interrupt-hands-on.md`
- `2026-03-19-002-freertos-hands-on.md`
- `2026-03-19-003-nvic-isr-priority-critical-section.md`
- `2026-03-19-004-critical-section-and-priority-config.md`
- `2026-03-19-005-embedded-full-process-with-freertos.md`
- `2026-03-19-006-embedded-freertos-data-structures.md`

建议后续把这篇当成总路线图，其他文件当成分模块深入笔记。

---

# 关键要点

1. 嵌入式学习一定要按层次推进，不能只盯着某个库函数。
2. 最核心的基础是：C、启动流程、寄存器、外设、中断、并发。
3. RTOS 要建立在裸机和中断基础之上。
4. 每学一个模块，都要做一个小实验验证。
5. 真正的成长来自“写驱动 + 查问题 + 做项目”，不是只看教程。

---

# 实战建议

如果你要把这套路线尽快转成能力，建议你后面按这个节奏继续让我帮你生成笔记：

1. `uart` 专题
2. `timer` 专题
3. `dma` 专题
4. `i2c` 专题
5. `spi` 专题
6. `adc` 专题
7. `watchdog` 专题
8. `bootloader` 专题

你每次给我一个主题，我就继续按这个目录规范，帮你沉淀成一篇可复习、可面试、可实战的学习文件。
